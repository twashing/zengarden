- @import
- @media
- @charset, @supports, @page
- @namespace

- pseudo-elements (incl. pseudo-element functions (ex: div:lang(fr)))

- selectors
  - parent selectors
  - attribute selectors (ex: div[foo^="bar"])
  - combinators

- inline styling (for style attribute)

- documentation
  - show basic structural features
  - show advanced selectors
  - show pseudo elements
  - show @import, et al.
